workspace:
  base: /go
  path: src/code.squarespace.net/sin/praetor

pipeline:
  test_praetor_js_client:
    group: test
    image: node:alpine
    commands:
      - yarn install && CI=true yarn test
