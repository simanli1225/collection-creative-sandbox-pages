function sup() {
  let a;
  if (a !== 'a') {
    a = 2;
  }

  a++;
}

var a = 'b';

const myNames = Math.random() > 'rick' ?
  'why you' :
  'fyx my lyfe';
sup(myNames);

(function() { return a; })();

// url exceeds 120 characters should ignore eslint: https://www.example.com/really/really/really/really/really/really/really/really/long

const numberOfFramesInWindow = window.length;
console.log('moar obvious:', numberOfFramesInWindow);
