"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getHashKeyFromOptions = exports.isPOJO = exports.isString = exports.getObj = void 0;
var hash_1 = require("./hash");
var getObj = function (obj, key, fallbackValue) {
    if (!key.length) {
        return fallbackValue;
    }
    if (typeof key === 'string') {
        key = key.split('.');
    }
    var lookup = obj[key[0]];
    if (typeof lookup === 'string') {
        return lookup;
    }
    if (typeof lookup === 'object' && lookup !== null) {
        if (key.length <= 1) {
            return lookup;
        }
        return (0, exports.getObj)(lookup, key.slice(1), fallbackValue);
    }
    return fallbackValue;
};
exports.getObj = getObj;
var isString = function (value) { return typeof value === 'string'; };
exports.isString = isString;
var isPOJO = function (value) {
    return value && typeof value === 'object' && Object.getPrototypeOf(value) === Object.prototype;
};
exports.isPOJO = isPOJO;
var getHashKeyFromOptions = function (value, opts) {
    var project = opts && opts.project ? opts.project : '';
    var notes = opts && opts.notes ? opts.notes : '';
    return (0, hash_1.hash)(project, value, notes);
};
exports.getHashKeyFromOptions = getHashKeyFromOptions;
