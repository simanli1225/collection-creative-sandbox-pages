const { createJestConfig } = require('@sqs/febs-test-config');

module.exports = createJestConfig(baseConfig => {
  return {
    ...baseConfig,
    testEnvironment: 'jsdom',
    displayName: '@sqs/session-info'
  };
});