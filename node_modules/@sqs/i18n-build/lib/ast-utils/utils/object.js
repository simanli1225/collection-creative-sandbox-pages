"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getObjectPropertyByName = getObjectPropertyByName;
const tslib_1 = require("tslib");
const t = tslib_1.__importStar(require("@babel/types"));
/**
 * Returns property node by name
 *
 * @param {ObjectExpression} obj
 * @param {string} name
 * @returns {ObjectProperty}
 */
function getObjectPropertyByName(obj, name) {
    return (t.isObjectExpression(obj) &&
        obj.properties.find((n) => {
            const { key } = n;
            if (t.isIdentifier(key)) {
                return key.name === name;
            }
            else if (t.isStringLiteral(key)) {
                return key.value === name;
            }
            return false;
        }));
}
