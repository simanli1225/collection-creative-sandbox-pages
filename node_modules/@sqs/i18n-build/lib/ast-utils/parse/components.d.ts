import * as t from '@babel/types';
import { ParseMetaData } from '../../modules/extract';
/**
 * Finds any invalid usage errors. If none, parses a valid Plural component
 *
 * @param {JSXElement} node
 * @returns {translationInstance}
 */
export declare const parsePluralComponent: (node: t.JSXElement) => ParseMetaData;
/**
 * Finds any invalid usage errors. If none, parses a valid T component
 *
 * @param {JSXElement} node
 * @returns {translationInstance}
 */
export declare const parseTComponent: (node: t.JSXElement) => ParseMetaData;
