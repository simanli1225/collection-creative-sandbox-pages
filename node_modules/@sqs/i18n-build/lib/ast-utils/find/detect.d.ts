import * as t from '@babel/types';
/**
 * Determine whether we have wrappable children
 * IE:
 *
 * <div>
 *   Hello, <a href="#">Name</a>, Here we are.
 * </div>
 *
 * =>
 *
 * <div><T>Hello, <a href="#">Name</a>, Here we are.</T></div>
 *
 * <div>
 *   Hello. <div>What</div> Man
 * </div>
 * =>
 * <div>
 *   <T>Hello</T>
 *   <div>
 *     <T>What</T>
 *   </div>
 *   <T>Man</T>
 * </div>
 * @param  {JSXElement} node a JSXElement node
 * @return {boolean} whether the node has wrappable children
 */
declare const _default: (node: t.Node) => boolean;
export default _default;
