import { ExtractResultWithPaths } from '../modules/extract';
type ParseResults = {
    strings: ExtractResultWithPaths[];
    errors: string;
};
export default function parse(src: string, pathname: string): ParseResults;
export {};
