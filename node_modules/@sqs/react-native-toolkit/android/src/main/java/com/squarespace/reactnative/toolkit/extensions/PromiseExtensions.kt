package com.squarespace.reactnative.toolkit.extensions

import com.facebook.react.bridge.Promise

/**
 * Resolves a promise without providing any result.
 *
 * @see Promise.resolve
 */
fun Promise.resolve() {
  this.resolve(null)
}
