declare enum SubscriptionRequestState {
    PENDING = "pending",
    PROCESSING = "processing",
    SUCCESSFUL = "successful",
    FAILED = "failed",
    REQUIRES_ACTION = "requires_action",
    REQUIRES_ACTION_PASSED = "requires_action_passed"
}
export default SubscriptionRequestState;
