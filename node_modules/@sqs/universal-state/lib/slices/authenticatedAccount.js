import { createSlice } from '@reduxjs/toolkit';
import { actions as bootstrapActions } from './bootstrap';
export var initialState = null;
var slice = createSlice({
  name: 'authenticatedAccount',
  initialState: initialState,
  reducers: {},
  extraReducers: builder => {
    builder.addCase(bootstrapActions.hardBootstrap, (_, action) => action.payload.authenticatedAccount);
    builder.addCase(bootstrapActions.softBootstrap, (state, action) => state || action.payload.authenticatedAccount);
  }
});
export var reducer = slice.reducer;
export var selectors = {
  selectAuthenticatedAccount: _ref => {
    var authenticatedAccount = _ref.authenticatedAccount;
    return authenticatedAccount;
  }
};