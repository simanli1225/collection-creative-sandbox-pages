import reducer from './reducer';
import { getRootWindow } from './setup';
import { initialState } from './state';
import { createStore } from './transport';
export default function () {
  return createStore(reducer, initialState, getRootWindow());
}