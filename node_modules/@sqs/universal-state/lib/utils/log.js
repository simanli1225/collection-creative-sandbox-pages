import "core-js/modules/es.array.concat.js";
import "core-js/modules/es.array.iterator.js";
import "core-js/modules/web.dom-collections.iterator.js";
var PREFIX = '[universal-state]';
function addPrefix() {
  var arg1 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';
  for (var _len = arguments.length, rest = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    rest[_key - 1] = arguments[_key];
  }
  if (typeof arg1 === 'string') {
    return ["".concat(PREFIX, " ").concat(arg1), ...rest];
  }
  return [PREFIX, arg1, ...rest];
}
export default {
  info() {
    console.info(...addPrefix(...arguments));
  },
  warn() {
    console.warn(...addPrefix(...arguments));
  },
  error() {
    console.error(...addPrefix(...arguments));
  }
};