import { UniversalStateEvent } from './protocol';
import { Store } from './storeTypes';
export default class Bridge<S, A> {
    private store;
    private consumerWindow;
    private unsubscribeFromStore;
    private stopListeningForEvents;
    constructor(store: Store<S, A>, consumerWindow: Window);
    teardown(): void;
    handleEvent: (event: UniversalStateEvent<S, A>) => void;
    sendStateUpdate: () => void;
}
//# sourceMappingURL=Bridge.d.ts.map