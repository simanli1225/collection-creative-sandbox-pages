var rootWindow = window;
var hasBeenAccessed = false;
export function getRootWindow() {
  hasBeenAccessed = true;
  return rootWindow;
}
export function setRootWindow(newValue) {
  if (hasBeenAccessed) {
    throw new Error('setRootWindow() called, but Universal State has already initialized.\n' + "setRootWindow() must be called before '@sqs/universal-state' is imported.\n" + 'See https://goto/universal-state/api for more details.');
  }
  rootWindow = newValue;
}