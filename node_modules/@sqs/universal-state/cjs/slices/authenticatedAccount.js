"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.selectors = exports.reducer = exports.initialState = void 0;
var _toolkit = require("@reduxjs/toolkit");
var _bootstrap = require("./bootstrap");
var initialState = exports.initialState = null;
var slice = (0, _toolkit.createSlice)({
  name: 'authenticatedAccount',
  initialState: initialState,
  reducers: {},
  extraReducers: builder => {
    builder.addCase(_bootstrap.actions.hardBootstrap, (_, action) => action.payload.authenticatedAccount);
    builder.addCase(_bootstrap.actions.softBootstrap, (state, action) => state || action.payload.authenticatedAccount);
  }
});
var reducer = exports.reducer = slice.reducer;
var selectors = exports.selectors = {
  selectAuthenticatedAccount: _ref => {
    var authenticatedAccount = _ref.authenticatedAccount;
    return authenticatedAccount;
  }
};