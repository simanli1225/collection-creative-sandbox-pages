/**
 * Interceptors are methods that can be applied during a request or response lifecycle, triggering
 * before .then & .catch handlers.
 * A Request Interceptor will receive the request config as a payload.
 * A Response Interceptor will receive the response object as a payload.
 * See: https://github.com/axios/axios#interceptors
 */

export { default as crumbInterceptor } from './crumbInterceptor';
export { default as paramsSerializerInterceptor } from './paramsSerializerInterceptor';