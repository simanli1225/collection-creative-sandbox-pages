import BaseClient from '../base';
import { ClientConfig, DefaultPayload } from '../types';
export default class EventsClient extends BaseClient {
    constructor(config: ClientConfig, defaultPayload?: DefaultPayload);
    protected _generatePayload(event: any): {
        eventContent: any;
        eventSchemaName: any;
    };
    protected _warnOfOverwrittenFields(defaultAndEvent: any, autoPopulatedFields: any): void;
    protected _generateUrl(): string;
    spawnTracker(defaults?: DefaultPayload): EventsClient;
    protected _validateEvent(payload: any): Promise<void>;
}
export declare class SiteVisitorClient extends EventsClient {
    constructor(config: ClientConfig, defaultPayload?: DefaultPayload);
}
//# sourceMappingURL=index.d.ts.map