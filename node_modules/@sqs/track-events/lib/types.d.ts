export declare enum SourceEnvironment {
    DEV = "dev",
    QA = "qa",
    STAGING = "staging",
    PROD = "prod"
}
export declare enum EventSource {
    WEB = "WEB",
    IOS = "IOS",
    ANDROID = "ANDROID",
    SERVER = "SERVER"
}
interface Cookie {
    daysToStore: number;
    domains: string[];
    path: string;
    secure: boolean;
}
export interface ClientConfig {
    customSchemaName: string;
    sourceEnvironment?: SourceEnvironment;
    devBaseDomainUrl?: string;
    fireEvents?: boolean;
    logging?: boolean;
    useBeacon?: boolean;
    validateMode?: boolean;
    setAnalyticsCookie?: boolean;
}
export interface ClientInternalConfig extends ClientConfig {
    storageKey: string;
    expirationKey: string;
    cookie: Cookie;
    url: string;
}
export type DefaultPayload = Partial<{
    context_website_id: string;
    member_account_id: string;
    resolved_locale: string;
    website_locale: string;
    member_account_locale: string;
    frontsite_language: string;
}> & Record<string, unknown>;
export {};
//# sourceMappingURL=types.d.ts.map