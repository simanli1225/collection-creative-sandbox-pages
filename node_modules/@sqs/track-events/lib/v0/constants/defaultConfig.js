import { maybeWindow } from '../../util';
var getDefaultConfig = function getDefaultConfig() {
  var _maybeWindow$location, _maybeWindow, _maybeWindow2;
  var HOST = (_maybeWindow$location = (_maybeWindow = maybeWindow()) === null || _maybeWindow === void 0 ? void 0 : _maybeWindow.location.origin) !== null && _maybeWindow$location !== void 0 ? _maybeWindow$location : '';
  return {
    url: "".concat(HOST, "/api/events/RecordEvent"),
    cookie: {
      daysToStore: 390,
      domains: ['squarespace.com', 'squarespace.net', 'sqsp.net'],
      path: '/',
      secure: ((_maybeWindow2 = maybeWindow()) === null || _maybeWindow2 === void 0 ? void 0 : _maybeWindow2.location.protocol.indexOf('https')) > -1
    }
  };
};
export { getDefaultConfig };