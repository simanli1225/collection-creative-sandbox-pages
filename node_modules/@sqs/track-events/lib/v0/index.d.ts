import BaseClient from '../base';
import { ClientConfig, DefaultPayload } from '../types';
export default class EventsClient extends BaseClient {
    constructor(config: ClientConfig, defaultPayload?: DefaultPayload);
    protected _generatePayload(event: any): any;
    track(eventName: string, event?: object, optionalTopLevelArgs?: object): Promise<void>;
    protected _sendXhr({ eventName, payload }: {
        eventName: any;
        payload: any;
    }, optionalTopLevelArgs: any, url?: string): Promise<void>;
    spawnTracker(defaults?: DefaultPayload): EventsClient;
}
//# sourceMappingURL=index.d.ts.map