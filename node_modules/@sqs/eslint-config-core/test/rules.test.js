describe('Validating rules', function () {
  it('does not throw a parse error', () => {
    let index;
    expect(() => {
      index = require('..');
    }).not.toThrow();
    expect(index).toBeTruthy();
  });
});
