import { LanguageTag } from './languagetag';
/**
 * Low-level parsing of a language tag. No resolution is performed.
 *
 * @public
 */
export declare const parseLanguageTag: (str: string) => LanguageTag;
