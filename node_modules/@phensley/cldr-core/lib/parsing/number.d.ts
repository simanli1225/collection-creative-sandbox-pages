export declare const enum NumberField {
    MINUS = 0,
    PERCENT = 1,
    CURRENCY = 2,
    NUMBER = 3,
    EXPONENT = 4,
    PLUS = 5
}
export interface NumberPattern {
    nodes: NumberNode[];
    minInt: number;
    maxFrac: number;
    minFrac: number;
    priGroup: number;
    secGroup: number;
}
export type NumberNode = string | NumberField;
export declare const parseNumberPattern: (raw: string) => NumberPattern[];
