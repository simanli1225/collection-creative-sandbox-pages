import { DayPeriodType } from '@phensley/cldr-types';
import { Bundle } from '../../resource/bundle';
export declare class DayPeriodRules {
    private cache;
    constructor(cacheSize: number);
    get(bundle: Bundle, minutes: number): DayPeriodType | undefined;
}
