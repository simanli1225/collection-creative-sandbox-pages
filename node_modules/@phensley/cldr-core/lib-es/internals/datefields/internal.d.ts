import { RelativeTimeFieldType } from '@phensley/cldr-types';
import { DecimalArg } from '@phensley/decimal';
import { DateFieldInternals, Internals } from '../internals';
import { RelativeTimeFieldFormatOptions } from '../../common';
import { Bundle } from '../../resource';
import { ContextTransformInfo, NumberParams } from '../../common/private';
/**
 * @internal
 */
export declare class DateFieldInternalsImpl implements DateFieldInternals {
    private internals;
    private relativeTimes;
    constructor(internals: Internals);
    formatRelativeTimeField(bundle: Bundle, value: DecimalArg, field: RelativeTimeFieldType, options: RelativeTimeFieldFormatOptions, params: NumberParams, transform: ContextTransformInfo): string;
}
