const sl_notr_start = require( './translate' )
const sl_notr_end = require( './translate' )

sl_notr_start()
const CURRENCY = 'currency'
const LANGUAGE = 'language'
sl_notr_end()

export default {
  CURRENCY,
  LANGUAGE,
}
